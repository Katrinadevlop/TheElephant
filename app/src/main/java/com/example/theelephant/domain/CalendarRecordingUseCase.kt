package com.example.theelephant.domain

import com.example.theelephant.data.repository.ScheduleRepository

class CalendarRecordingUseCase(private val scheduleRepository: ScheduleRepository) {
    fun getAllSchedule():Boolean{
        return
    }
}