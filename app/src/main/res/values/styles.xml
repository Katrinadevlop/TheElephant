<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="wrap_contents">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Стиль для TextView заголовки -->
    <style name="TitleText" parent="wrap_contents">
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Стиль для EditText -->
    <style name="EditTextStyle">
        <item name="android:layout_width">350dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:ems">10</item>
    </style>

    <!-- Стиль для Button -->
    <style name="ButtonStyle" parent="wrap_contents">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Стиль для обычного TextView информация о пользователе -->
    <style name="NormalText" parent="wrap_contents">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">5dp</item>
    </style>

    <!-- Стиль для TextView ссылки -->
    <style name="LinkText" parent="wrap_contents">
        <item name="android:textColor">@color/blue</item>
    </style>

    <!-- Стиль для обычного TextView для настроек приложения -->
    <style name="MenuTextView" parent="wrap_contents">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:padding">8dp</item>
    </style>

</resources>
